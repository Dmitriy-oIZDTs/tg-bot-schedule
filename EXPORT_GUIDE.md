# Руководство по экспорту данных 📊

## Введение

Бот теперь поддерживает экспорт расписания и логов в формати Excel, что позволяет удобно анализировать и делиться данными.

---

## Быстрый старт

### 1. Экспорт расписания вашей группы

```
/export_schedule
```

Экспортирует расписание на 30 дней в Excel для вашей группы.

### 2. Экспорт расписания конкретной группы

```
/export_schedule БПИ-24
```

Экспортирует расписание группы БПИ-24 на 30 дней.

### 3. Экспорт расписания на определенный период

```
/export_schedule БПИ-24 14
```

Экспортирует расписание группы БПИ-24 на последние 14 дней.

### 4. Экспорт всего расписания (только для администраторов)

```
/export_all_schedule
```

Экспортирует расписание всех групп на 30 дней.

### 5. Экспорт логов действий

```
/export_logs
```

Экспортирует логи последних 24 часов в CSV.

```
/export_logs 7 excel
```

Экспортирует логи последних 7 дней в Excel.

---

## Формат файлов

### Excel-файлы расписания

**Структура:**
- 📊 Заголовок с указанием группы и периода
- 📋 Таблица с колонками:
  - Группа
  - Дата
  - День недели
  - Номер пары
  - Время
  - Предмет
  - Тип (лекция, практика, лабораторная)
  - Преподаватель
  - Аудитория

**Пример:**

| Группа | Дата | День | Пара | Время | Предмет | Тип | Преподаватель | Аудитория |
|--------|------|------|------|-------|---------|-----|---------------|-----------|
| БПИ-24 | 2024-12-09 | Понедельник | 1 | 09:00-10:30 | Программирование | lecture | Иванов И.И. | 101 (Корпус 1) |

**Форматирование:**
- ✅ Цветные заголовки (зеленые)
- ✅ Границы таблицы
- ✅ Автоматическая ширина колонок
- ✅ Выравнивание текста

### Excel-файлы логов

**Структура:**
- 📊 Заголовок "Логи действий"
- 📋 Таблица с колонками:
  - ID пользователя
  - Telegram ID
  - Имя пользователя
  - Действие
  - Детали
  - Дата/Время

**Пример:**

| ID пользователя | Telegram ID | Имя пользователя | Действие | Детали | Дата/Время |
|-----------------|-------------|------------------|----------|--------|------------|
| 1 | 123456789 | ivanov | view_schedule | Группа: БПИ-24 | 2024-12-09 10:30:45 |

**Форматирование:**
- ✅ Цветные заголовки (синие)
- ✅ Границы таблицы
- ✅ Автоматическая ширина колонок

---

## Команды подробно

### /export_schedule

**Синтаксис:**
```
/export_schedule [номер_группы] [количество_дней]
```

**Параметры:**
- `номер_группы` (опционально): Номер группы (например, БПИ-24)
  - Если не указана, используется ваша группа
  - Если у вас нет группы, необходимо указать
- `количество_дней` (опционально): Количество дней для экспорта
  - По умолчанию: 30 дней
  - Минимум: 1 день
  - Максимум: 365 дней

**Примеры:**
```
/export_schedule                  # Расписание вашей группы на 30 дней
/export_schedule БПИ-24           # Расписание БПИ-24 на 30 дней
/export_schedule БПИ-24 7         # Расписание БПИ-24 на 7 дней
/export_schedule ИВТ-21 14        # Расписание ИВТ-21 на 14 дней
```

**Вывод:**
- ✅ Excel-файл `schedule_[группа]_[дата].xlsx`
- ✅ Сообщение с информацией о периоде и количестве занятий

**Доступность:**
- ✅ Все пользователи (для своей группы)
- ✅ Администраторы (для любой группы)

---

### /export_all_schedule

**Синтаксис:**
```
/export_all_schedule [количество_дней]
```

**Параметры:**
- `количество_дней` (опционально): Количество дней для экспорта
  - По умолчанию: 30 дней

**Примеры:**
```
/export_all_schedule           # Расписание всех групп на 30 дней
/export_all_schedule 7         # Расписание всех групп на 7 дней
```

**Вывод:**
- ✅ Excel-файл `schedule_все_группы_[дата].xlsx`
- ✅ Сообщение с информацией о периоде и общем количестве занятий

**Доступность:**
- ✅ Только администраторы
- ✅ Только разработчики

---

### /export_logs

**Синтаксис:**
```
/export_logs [количество_дней] [формат]
```

**Параметры:**
- `количество_дней` (опционально): Количество дней для экспорта
  - По умолчанию: 1 день
- `формат` (опционально): Формат файла
  - `excel` или `xlsx` - Excel файл
  - `csv` - CSV файл (по умолчанию)

**Примеры:**
```
/export_logs                   # Логи за последние сутки в CSV
/export_logs 7                 # Логи за последние 7 дней в CSV
/export_logs 7 excel           # Логи за последние 7 дней в Excel
/export_logs 30 xlsx           # Логи за последний месяц в Excel
/export_logs 1 csv             # Логи за сутки в CSV
```

**Вывод:**
- ✅ Файл `user_actions_[дата].csv` или `.xlsx`
- ✅ Сообщение с информацией о периоде, количестве записей и формате

**Доступность:**
- ✅ Только администраторы
- ✅ Только разработчики

---

### /logs (классическая команда)

**Синтаксис:**
```
/logs [количество_дней]
```

**Параметры:**
- `количество_дней` (опционально): Количество дней для экспорта
  - По умолчанию: 1 день

**Примеры:**
```
/logs                          # Логи за последние сутки в CSV
/logs 7                        # Логи за последние 7 дней в CSV
```

**Вывод:**
- ✅ CSV файл `user_actions_[дата].csv`
- ✅ Сообщение с информацией о периоде

**Доступность:**
- ✅ Только администраторы

---

## Структура файлов

### Директория reports

Все экспортированные файлы сохраняются в директорию `reports/`:

```
reports/
├── schedule_БПИ-24_20241209_143022.xlsx
├── schedule_ИВТ-21_20241209_144530.xlsx
├── schedule_все_группы_20241209_145100.xlsx
├── user_actions_20241209_010150.csv
├── user_actions_20241209_143022.xlsx
└── ...
```

**Имена файлов:**
- Расписание группы: `schedule_[группа]_[ггггммдд]_[ччммсс].xlsx`
- Расписание всех: `schedule_все_группы_[ггггммдд]_[ччммсс].xlsx`
- Логи: `user_actions_[ггммдд]_[ччммсс].[csv|xlsx]`

---

## Примеры использования

### Сценарий 1: Экспорт расписания для отправки

1. **Получить расписание своей группы на неделю:**
   ```
   /export_schedule БПИ-24 7
   ```

2. **Получить файл и отправить коллегам**
   - Файл скачивается в формате Excel
   - Можно открыть в Excel, Google Sheets, или любом другом приложении

### Сценарий 2: Администратор экспортирует все расписание

1. **Получить полное расписание на семестр:**
   ```
   /export_all_schedule 120
   ```

2. **Анализировать в Excel:**
   - Фильтровать по группам
   - Сортировать по датам
   - Считать количество занятий

### Сценарий 3: Проверка активности пользователей

1. **Получить логи за неделю:**
   ```
   /export_logs 7 excel
   ```

2. **Анализировать:**
   - Кто и когда используя бот
   - Какие команды вызываются чаще всего
   - Построить диаграммы активности

---

## Технические детали

### Используемые библиотеки

- **openpyxl** - Создание и работа с Excel файлами
- **pandas** - Работа с табличными данными (установлена в зависимостях)
- **csv** - Встроенная библиотека для CSV

### Реализованные функции

**reporting.py:**
- `export_schedule_to_excel()` - Экспорт расписания в Excel
- `export_user_actions_to_excel()` - Экспорт логов в Excel
- `export_user_actions_to_csv()` - Экспорт логов в CSV
- `get_day_name()` - Преобразование номера дня недели

**db_manager.py:**
- `get_all_schedule_range()` - Получение расписания за период

**handlers.py:**
- `cmd_export_schedule()` - Обработчик `/export_schedule`
- `cmd_export_all_schedule()` - Обработчик `/export_all_schedule`
- `cmd_export_logs()` - Обработчик `/export_logs`

---

## Решение проблем

### Проблема: "Group not found"
**Решение:** Убедитесь, что номер группы написан правильно и существует в системе.

### Проблема: "No schedule found"
**Решение:** На указанный период или для группы расписание может быть не создано. Проверьте:
- Дату в системе
- Наличие расписания в БД
- Используйте `/export_schedule БПИ-24 30` для проверки

### Проблема: "Access denied"
**Решение:** Команда доступна только администраторам. Обратитесь к разработчику.

### Проблема: "File size is too large"
**Решение:** Telegram имеет ограничение на размер файла (50 МБ). Попробуйте:
- Уменьшить период (например, вместо 365 дней указать 30)
- Экспортировать по отдельным группам

---

## Советы и рекомендации

✅ **Лучшие практики:**
- Экспортируйте регулярно для резервного копирования
- Используйте Excel для анализа и визуализации данных
- Сортируйте по датам для лучшей читаемости
- Используйте фильтры в Excel для поиска информации

⚠️ **Что нужно учитывать:**
- Экспорт может занять время для больших объемов данных
- Файлы сохраняются на сервере в директории `reports/`
- Убедитесь, что у сервера достаточно дискового пространства

---

## Контакты и поддержка

Если у вас есть вопросы или проблемы:
- Используйте `/help` для справки
- Свяжитесь с администратором бота
- Проверьте логи в `/export_logs`

---

**Версия:** 1.0  
**Дата последнего обновления:** Декабрь 2024  
**Автор:** Романов Дмитрий Владимирович
