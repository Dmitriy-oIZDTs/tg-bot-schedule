# Руководство по импорту и экспорту расписания

## 📤 ЭКСПОРТ

### Команды экспорта

#### 1. Экспорт расписания одной группы
```
/export_schedule [номер_группы] [дней]
```

**Примеры:**
```
/export_schedule БПИ-24          # Расписание БПИ-24 на 30 дней
/export_schedule БПИ-24 14       # Расписание БПИ-24 на 14 дней
/export_schedule БПИ-25 7        # Расписание БПИ-25 на неделю
```

**Параметры:**
- `номер_группы` (опционально) - номер группы. Если не указан, берется группа пользователя
- `дней` (опционально) - количество дней периода (по умолчанию 30)

**Результат:** 📊 Excel файл с расписанием и красивым оформлением

---

#### 2. Экспорт расписания всех групп
```
/export_all_schedule [дней]
```

**Примеры:**
```
/export_all_schedule             # Всё расписание на 30 дней
/export_all_schedule 60          # Расписание на 2 месяца
```

⚠️ **Доступно только администраторам**

---

#### 3. Экспорт логов действий пользователей
```
/export_logs [дней] [формат]
```

**Примеры:**
```
/export_logs 7 excel             # Логи за неделю в Excel
/export_logs 30 csv              # Логи за месяц в CSV
/export_logs 1 xlsx              # Логи за день в Excel
```

**Параметры:**
- `дней` (опционально) - количество дней (по умолчанию 1)
- `формат` (опционально) - формат файла: `excel`, `xlsx`, `csv` (по умолчанию csv)

⚠️ **Доступно только администраторам**

---

#### 4. Классический экспорт логов (CSV)
```
/logs [дней]
```

**Примеры:**
```
/logs 7                          # Логи за неделю в CSV
/logs 30                         # Логи за месяц в CSV
```

⚠️ **Доступно только администраторам**

---

## 📥 ИМПОРТ

### Шаг 1: Получить шаблон
```
/get_template
```

Команда выдаст файл `schedule_template_YYYYMMDD_HHMMSS.xlsx` с правильной структурой.

**Структура шаблона:**

| Группа* | Дата* | Пара* | Начало* | Конец* | Предмет* | Тип* | Преподаватель | Аудитория |
|---------|-------|-------|--------|--------|----------|------|---------------|-----------|
| БПИ-24  | 2026-02-10 | 1 | 09:00 | 10:30 | Программирование | lecture | Иванов И.И. | 101 |
| БПИ-24  | 2026-02-10 | 2 | 10:45 | 12:15 | Алгоритмы | practice | Петров П.П. | 102 |

---

### Шаг 2: Заполнить данные

**Обязательные поля** (отмечены *)
- `Группа` - номер группы (должна существовать в БД)
- `Дата` - дата в формате YYYY-MM-DD (например, 2026-02-10)
- `Пара` - номер пары (1-6)
- `Начало` - время начала в формате HH:MM (например, 09:00)
- `Конец` - время конца в формате HH:MM (например, 10:30)
- `Предмет` - название предмета
- `Тип` - тип пары: `lecture`, `practice`, `lab`

**Опциональные поля:**
- `Преподаватель` - ФИО преподавателя (будет создан автоматически если не существует)
- `Аудитория` - номер аудитории (будет создана автоматически если не существует)

---

### Шаг 3: Загрузить расписание
```
/import_schedule
```

Затем отправьте заполненный Excel файл.

**Процесс импорта:**
1. ✅ Проверка структуры файла
2. ✅ Валидация всех данных
3. ✅ Автоматическое создание недостающих сущностей (преподаватели, аудитории, предметы)
4. ✅ Добавление/обновление расписания в БД

**Результат:** 📊 Отчет об импорте с информацией о количестве добавленных записей и ошибок

---

### Шаг 4: Проверить результат (опционально)
```
/schedule_stats
```

Показывает:
- Общее количество записей расписания
- Количество уникальных дат
- Количество групп
- Диапазон дат расписания

⚠️ **Доступно только разработчику**

---

## 🗑️ УДАЛЕНИЕ РАСПИСАНИЯ

### Удалить всё расписание группы
```
/clear_schedule БПИ-24
```

### Удалить расписание за период
```
/clear_schedule БПИ-24 2026-02-01 2026-02-28
```

⚠️ **Доступно только администраторам**

---

## 📋 Форматы файлов

### Excel (.xlsx)
- 📊 Красивое оформление с цветами
- 📐 Таблиці с границами
- 🔤 Автоширина колонок
- ✅ Лучше для просмотра и редактирования

### CSV (.csv)
- 📝 Простой текстовый формат
- ⚡ Быстрый экспорт
- 📊 Совместим с Excel и другими программами
- ✅ Удобен для скриптов обработки

---

## 🛠️ Решение проблем

### Проблема: Импорт показывает ошибку "Группа не найдена"
**Решение:** Убедитесь, что группа БПИ-24 существует в БД. Используйте точное название группы.

### Проблема: При экспорте выводится только один день
**Решение:** 
1. Используйте `/schedule_stats` для проверки данных в БД
2. Увеличьте количество дней в параметре экспорта
3. Проверьте диапазон дат в БД

### Проблема: Ошибка при загрузке файла
**Решение:**
1. Убедитесь, что используется формат .xlsx (не .xls)
2. Заполните все обязательные поля (отмечены *)
3. Проверьте даты в формате YYYY-MM-DD
4. Проверьте время в формате HH:MM (24-часовой формат)

### Проблема: Преподавателя не найдено
**Решение:** Система автоматически создает новых преподавателей при импорте.

### Проблема: Аудитория не задана
**Решение:** Аудитория опциональна. Если её не указать, занятие будет без конкретной аудитории.

---

## 📊 Примеры использования

### Сценарий 1: Новая группа
```
1. /get_template
2. Заполнить расписание для БПИ-26
3. /import_schedule + отправить файл
4. /export_schedule БПИ-26 30 (проверить результат)
```

### Сценарий 2: Обновление существующего расписания
```
1. /export_schedule БПИ-24 30 (экспортировать текущее)
2. Отредактировать в Excel
3. /clear_schedule БПИ-24 (удалить старое)
4. /import_schedule + отправить обновленный файл
```

### Сценарий 3: Резервная копия и анализ
```
1. /export_all_schedule 365 (годовой отчет)
2. /export_logs 30 excel (логи за месяц)
3. Сохранить файлы для архива
```

---

## ✅ Лучшие практики

1. ✅ Всегда используйте `/get_template` для получения правильной структуры
2. ✅ Проверьте форматирование дат (YYYY-MM-DD) перед импортом
3. ✅ Используйте `/schedule_stats` для проверки данных
4. ✅ Создавайте резервные копии перед очисткой расписания
5. ✅ При массовом импорте проверьте первые несколько строк на ошибки
6. ✅ Используйте точные названия групп и преподавателей

---

## 👥 Уровни доступа

| Команда | User | Admin | Developer |
|---------|------|-------|-----------|
| /export_schedule | ✅ | ✅ | ✅ |
| /export_all_schedule | ❌ | ✅ | ✅ |
| /export_logs | ❌ | ✅ | ✅ |
| /logs | ❌ | ✅ | ✅ |
| /get_template | ❌ | ✅ | ✅ |
| /import_schedule | ❌ | ✅ | ✅ |
| /clear_schedule | ❌ | ✅ | ✅ |
| /schedule_stats | ❌ | ❌ | ✅ |

---

## 📞 Контакты для вопросов

При проблемах с импортом/экспортом проверьте логи:
```bash
tail -100 bot.log
```

Или используйте `/help` для справки по команде.
